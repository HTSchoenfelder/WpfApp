<UserControl x:Class="WpfApp.Views.MainContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Button DockPanel.Dock="Top" Height="Auto" Content="Load Sample" Command="{Binding LoadCommand}"/>
        <Button DockPanel.Dock="Top" Height="Auto" Content="Clear Text" Command="{Binding ClearCommand}"/>
        <TextBlock DockPanel.Dock="Bottom" Height="Auto" Text="{Binding SampleText}" FontSize="38" />
    </DockPanel>
</UserControl>
